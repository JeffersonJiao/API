<nav class="navbar navbar-expand-sm bg-dark navbar-dark navbar-toggleable-md">
    <div class="container">
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/topscores">Top Scorer</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/gameplay">How to play</a>
                </li>
                <% if(user){ %>
                <li class="nav-item">
                    <a class="nav-link" href="/game">Grammar Test</a>
                </li>    
                <% } %>
            </ul>   
            <ul class="navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#">
                        <% if(user ) {%>
                            <%= user.username %>
                        <% }else{ %>
                        Account
                        <% } %>
                    </a>
                    <div class="dropdown-menu">
                        <% if(!user) {%>
                        <a class="dropdown-item" href="/auth/login">Login</a>
                        <a class="dropdown-item" href="/register">Register</a>
                        <% }else{ %>
                        <a class="dropdown-item" href="/auth/logout">Logout</a>
                        <% } %>
                    </div>
                </li>
            </ul>

        </div>
    </div>    
</nav>